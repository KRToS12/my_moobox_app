import 'package:flutter/material.dart';

class RegistrosTab extends StatelessWidget {
  const RegistrosTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Listado de Fletes y Registros"));
  }
}